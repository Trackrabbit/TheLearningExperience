SET QUOTED_IDENTIFIER OFF
GO
SET ANSI_NULLS ON
GO
 CREATE PROC [dbo].[zDP_SOP50100SD] (@PROCORIG smallint, @USERID char(15), @SOPNUMBE char(21), @SOPTYPE smallint, @RowsAffected int OUT, @RID int = 0, @TN char(99) = 'TN') AS  set nocount on BEGIN IF @RID > 0 DELETE FROM .SOP50100 WHERE PROCORIG = @PROCORIG AND USERID = @USERID AND SOPNUMBE = @SOPNUMBE AND SOPTYPE = @SOPTYPE AND ( NOT EXISTS ( SELECT 1 FROM tempdb.dbo.DEX_LOCK WHERE row_id = @RID AND table_path_name = @TN ) ) ELSE DELETE FROM .SOP50100 WHERE PROCORIG = @PROCORIG AND USERID = @USERID AND SOPNUMBE = @SOPNUMBE AND SOPTYPE = @SOPTYPE SELECT @RowsAffected = @@rowcount END set nocount off    
GO
GRANT EXECUTE ON  [dbo].[zDP_SOP50100SD] TO [DYNGRP]
GO
