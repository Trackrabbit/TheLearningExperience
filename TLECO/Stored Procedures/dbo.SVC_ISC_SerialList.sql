SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
 CREATE Procedure [dbo].[SVC_ISC_SerialList]  (  @CUSTNMBR char(15), @ADDRESSID char(15)  )  As SELECT DISTINCT EQUIPID, SERLNMBR , IV00101.ITEMNMBR, ITEMDESC , WARREND, SLRWEND  FROM  SVC00300 INNER JOIN IV00101 ON (SVC00300.ITEMNMBR = IV00101.ITEMNMBR)   WHERE SVC00300.CUSTNMBR = @CUSTNMBR and SVC00300.ADRSCODE = @ADDRESSID  ORDER BY SERLNMBR   return   
GO
GRANT EXECUTE ON  [dbo].[SVC_ISC_SerialList] TO [DYNGRP]
GO
