SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
 CREATE Procedure [dbo].[SVC_ISC_ItemList] As SELECT distinct ITEMNMBR, ITEMDESC, ITEMTYPE FROM IV00101 WHERE (ITEMTYPE <> 5) AND (ITEMTYPE <> 6) AND (ITEMTYPE <> 4)  ORDER BY ITEMNMBR   return    
GO
GRANT EXECUTE ON  [dbo].[SVC_ISC_ItemList] TO [DYNGRP]
GO
